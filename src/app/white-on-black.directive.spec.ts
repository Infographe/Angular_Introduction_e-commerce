import { WhiteOnBlackDirective } from './white-on-black.directive';

describe('WhiteOnBlackDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteOnBlackDirective();
    expect(directive).toBeTruthy();
  });
});
